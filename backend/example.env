# ==========================================================
# Environment Variables (for sensitive or environment-specific configuration)
# DO NOT commit this file to public repositories!
# ==========================================================

# 1. SERVER CONFIGURATION
# The port on which your Node.js server will run.
PORT=5001

# 2. DATABASE CONFIGURATION (MongoDB Atlas Connection URI)
# Contains the full connection string for Mongoose.
MONGO_DB_URI=your_mongodb_connection_uri
# Note: The 'appName=Cluster0' parameter is usually optional,
# but including the clean base URI is sufficient for Mongoose.

# 3. SECURITY & AUTHENTICATION
# Use a long, random, and complex string for production applications.
JWT_SECRET=your_jwt_secret_key
NODE_ENV=development

# 4. CLOUDINARY CONFIGURATION
CLOUDINAR_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINAR_API_KEY=your_cloudinary_api_key
CLOUDINAR_API_SECRET=your_cloudinary_api_secret
